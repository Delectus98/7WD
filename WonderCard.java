public interface Cloneable<T> {
    T clone();
}


public class WonderCard implements Cloneable<WonderCard>{
    private Action action;
    
    private static final HashMap<String, WonderCard> nativeWonders;
    public static natives(){
        nativeWonders.
    }
    public static WonderCard get(String name){
        if (nativeWonders.get(name))
    }
    
    public WonderCard(String name, Action action){
        
    }
    
    public WonderCard clone(){
        
    }
}
